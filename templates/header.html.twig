<nav class="navbar navbar-expand-lg navbar-custom sticky-top">
    <div class="container-fluid px-4">
        <a class="navbar-brand fw-bold text-uppercase tracking-widest" href="{{ path('app_home_index') }}" style="letter-spacing: 0.15em; font-size: 1.1rem;">
            <img src="{{ asset('images/logo.png') }}" alt="VICTORY ROAD FR" class="img-fluid" style="max-height: 40px; width: auto;">
        </a>

        <button class="navbar-toggler border-0" type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-center" id="navbarSupportedContent">
            <ul class="navbar-nav gap-1">
                <li class="nav-item">
                    <a class="nav-link nav-link-custom" href="{{ path('app_home_index') }}">Accueil</a>
                </li>
                
                <li class="nav-item dropdown">
                    <a class="nav-link nav-link-custom dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Pokédex
                    </a>
                    <ul class="dropdown-menu dropdown-menu-custom border-0 shadow-sm mt-2">
                        <li><a class="dropdown-item" href="{{ path('app_pokedex_index') }}">Tous les Pokémon</a></li>
                        <li><hr class="dropdown-divider"></li>
                        {% for i in 1..9 %}
                            <li><a class="dropdown-item" href="{{ path('app_pokedex_index', {'generation': i}) }}">Génération {{ i }}</a></li>
                        {% endfor %}
                    </ul>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-link-custom" href="{{ path('app_dresseur_index') }}">Dresseurs</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-link-custom" href="{{ path('app_arene_index') }}">Arènes</a>
                </li>
            </ul>
        </div>
        
    </div>
</nav>
