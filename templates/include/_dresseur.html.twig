{% for dresseur in dresseurs %}
    <div class="col-12 col-md-6 col-lg-2">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{ dresseur.nom }}</h5>
                <p class="card-text truncate-2-lines">{{ dresseur.description }}</p>
                <a href="#" class="btn btn-primary">Voir tout</a>
            </div>
        </div>
    </div>
{% endfor %}
